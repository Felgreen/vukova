# Dependencies (reinstalled inside Docker)
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js build output (rebuilt inside Docker)
.next
out

# Git
.git
.gitignore

# Environment files (injected at runtime via docker-compose, never baked in)
.env
.env.*
*.local

# Dev tools
.DS_Store
*.pem
README.md

# Test/coverage
coverage
